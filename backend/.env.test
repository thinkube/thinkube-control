# Test environment variables
PROJECT_NAME="Thinkube Control Test"
FRONTEND_URL="http://localhost:8080"
BACKEND_CORS_ORIGINS='["http://localhost:8080"]'
DOMAIN_NAME="test.com"

# Test Keycloak settings (not used in tests but required by settings)
KEYCLOAK_URL="https://auth.test.com"
KEYCLOAK_REALM="test"
KEYCLOAK_CLIENT_ID="test-client"
KEYCLOAK_CLIENT_SECRET="test-secret"

# Test database settings - PostgreSQL in cluster
# DATABASE_URL will be constructed in the test script using POSTGRES_PASSWORD
POSTGRES_HOST="postgresql-official.postgres.svc.cluster.local"
POSTGRES_PORT="5432"
POSTGRES_DB="thinkube_control_test"
# POSTGRES_USER will be set from ADMIN_USERNAME environment variable

# Test service URLs (required by settings)
SEAWEEDFS_URL="https://seaweed.test.com"
HARBOR_URL="https://harbor.test.com"
GITEA_URL="https://git.test.com"
ARGOCD_URL="https://argocd.test.com"
ARGO_WORKFLOWS_URL="https://argo.test.com"

# Test secret key
SECRET_KEY="test-secret-key-for-testing-only"